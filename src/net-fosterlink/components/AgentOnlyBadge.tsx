import { Badge } from "@/components/ui/badge"

export const AgentOnlyBadge = () => {
    return (
        <Badge variant="outline" className="bg-primary/10 text-primary border-primary/30 dark:bg-primary/30 dark:text-primary-foreground dark:border-primary/50">Agency Representative Only</Badge>
    )
}