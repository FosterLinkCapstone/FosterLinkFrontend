import { Badge } from "@/components/ui/badge"

export const AgentOnlyBadge = () => {
    return (
        <Badge variant="outline" className="bg-primary/10 text-primary border-primary/30">Agency Representative Only</Badge>
    )
}